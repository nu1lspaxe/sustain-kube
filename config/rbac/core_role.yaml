apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: manager-core-access
rules:
- apiGroups: [""]
  resources:
  - secrets
  - configmaps
  - pods
  - services
  - endpoints
  verbs: ["get", "list", "watch"]
